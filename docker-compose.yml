services:
  frontend:
    build: .
    ports:
      - "80:80"
    environment:
      - BACKEND_URL=http://host.docker.internal:8001
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
